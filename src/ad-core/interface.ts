export interface DOM {
  // 容器
  $container: HTMLElement | undefined,
  // 广告实体
  $ad: HTMLElement | undefined,
  // 关闭按钮
  $delIcon: HTMLElement | undefined
};
export interface OPTIONS {
  [`any`]: any;
};